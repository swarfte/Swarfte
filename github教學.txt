git init -->使用init命令移動到這個目錄並將目錄建立為本地端數據庫
mkdir xxx -->創建一個檔案夾
cd xxx -->移動到xxx檔案夾
git status -->在Git管理之下的目錄，可以使用status命令確認工作目錄與索引的狀態喔
git add <file> -->要將檔案加入到索引，可以使用add <file>命令。<file>是指向要加入索引的檔案 ,若要指向多個檔案，可用空白鍵將檔案們分開。
git add . -->使用「.」，表示可以把當前目錄下所有的檔案加入到索引。
